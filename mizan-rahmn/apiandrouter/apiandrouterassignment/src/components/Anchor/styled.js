import { styled } from "@mui/system";
import { Link } from "react-router-dom";

export const StyledLink = styled(Link)({
  color: "white",
  backgroundColor: "red",
  padding: 8,
  borderRadius: 4,
});
